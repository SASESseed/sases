# 兼容层：从 core.auth_service 导入所有功能，保持原有 import auth 用法不变
from core.auth_service import (
    SECRET_KEY,
    ALGORITHM,
    ACCESS_TOKEN_EXPIRE_MINUTES,
    SIGN_KEY_FILE,
    pwd_context,
    sign_state,
    verify_state,
    create_user,
    authenticate_user,
    create_access_token,
    get_user_by_id,
    add_credits,
    deduct_credits,
    verify_user_integrity,
    check_all_users_integrity,
    get_leaderboard,
    get_credit_ledger,
    add_system_message,
    get_system_messages,
    mark_messages_read,
    get_user_settings,
    update_user_settings,
    is_admin,
    set_admin,
    generate_state_signature,
    verify_state_signature,
    log_tamper_event,
)

# ========== SASES 环境配置示例 ==========
# 复制此文件为 .env 并填写实际值

# DeepSeek API 密钥（必填）
DEEPSEEK_API_KEY=

# 模型名称（可选，默认 deepseek-v4-flash）
MODEL_NAME=deepseek-v4-flash

# JWT 密钥（必填，建议使用随机字符串）
SASES_SECRET_KEY=change-me-to-a-long-random-string

# 节点通信令牌（用于节点间认证，生产环境必须设置）
SASES_NODE_TOKEN=change-me-to-another-random-string

# 节点 ID 和名称（用于节点注册与发现）
SASES_NODE_ID=node-001
SASES_NODE_NAME=My SASES Node

# 手动配置的对等节点列表（逗号分隔，可选）
# 例如：http://192.168.1.100:8001,http://192.168.1.101:8001
SASES_PEERS=

# 启用 mDNS 局域网自动发现（true/false，默认 false）
SASES_ENABLE_MDNS=true

# 服务端口（用于 mDNS 广播和节点间通信，默认 8001）
SASES_PORT=8001

# 视觉模型名称（默认 deepseek-v4-flash-vision-exp）
VISION_MODEL_NAME=deepseek-v4-flash-vision-exp

# 文件大小限制（字节，默认 5MB 图片，10MB 音频，25MB 视频）
MAX_IMAGE_SIZE=5242880
MAX_AUDIO_SIZE=10485760
MAX_VIDEO_SIZE=26214400

# 相似度阈值（0-1，默认 0.30）
SIMILARITY_THRESHOLD=0.30

# 积分规则（可选，默认值如下）
EXTERNAL_SEED_REWARD=5
MANUAL_POLLINATE_BASIC_REWARD=3
MANUAL_POLLINATE_EXPERT_REWARD=10
QUERY_DEDUCTION=2

# 数据库文件路径（可选，默认在项目根目录）
# DB_FILE=users.db
# KB_FILE=success_kb.json
# SEED_POOL_FILE=seed_tasks_external.jsonl
# MAIN_SEED_FILE=seed_tasks_new.jsonl